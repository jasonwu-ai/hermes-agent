# Synthetic grilled and roasted specification

This fixture represents fixed, non-personal specification bytes.

## Scope

- Build a local source-only candidate.
- Exercise deterministic tests in scratch storage.

## Exclusions

No GitHub write, live installation, gateway restart, deployment, activation, credential access, or worker launch.

```html
<script>hostile raw HTML is escaped by the renderer</script>
```
